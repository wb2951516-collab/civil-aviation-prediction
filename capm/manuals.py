"""算法使用手册（V1.1 精选算法集合）。

为每种保留算法提供面向一线业务人员的简明手册，涵盖：
适用数据特征 / 预测周期与业务场景 / 算法原理要点 / 参数设置建议 /
优缺点与局限性 / 典型使用案例。

手册数据为结构化文本，由界面"算法手册"入口点击查看。
"""

from __future__ import annotations

from typing import Dict, List

MANUALS: List[Dict[str, str]] = [
    {
        "id": "ensemble",
        "name": "融合模型（Holt-Winters + SARIMA）",
        "short": "默认推荐：多模型加权融合，兼顾季节形态与统计推断",
        "content": (
            "【适用数据特征】\n"
            "· 月度旅客运输量等具有明显年度季节性、长期趋势的历史序列（建议 36 个月以上）\n"
            "· 数据存在节假日波动（春运、暑运）与温和趋势变化的场景\n"
            "· 单模型各有短板、需要“取长补短”的预测任务\n\n"
            "【预测周期与业务场景】\n"
            "· 适合 12 个月内的中期滚动预测（月度粒度）\n"
            "· 典型场景：年度生产保障计划、月度运力排班、成本预算编制\n\n"
            "【算法原理要点】\n"
            "· 对 Holt-Winters 与 SARIMA 的预测值做加权平均：\n"
            "  预测值 = w1×HW + w2×SARIMA（w1 + w2 = 1）\n"
            "· 权重由回测实证确定（贝叶斯模型平均 BMA 优先），而非主观拍定\n"
            "· 若开启外部数据源，SARIMA 升级为 SARIMAX，自动引入 GDP/ASK 等因子\n\n"
            "【参数设置建议】\n"
            "· 权重：建议使用“自动推荐”（基于回测），一般 HW 0.4~0.6 / SARIMA 0.4~0.6\n"
            "· SARIMA 阶数：月度数据默认 (1,1,1)×(1,1,1,12)，数据量充足时开启自动调参\n"
            "· Holt-Winters：默认加法趋势+加法季节（trend=add, seasonal=add）\n\n"
            "【优缺点与局限性】\n"
            "· 优点：综合单模型优势，误差通常低于任一单模型；实现与维护成本低\n"
            "· 局限：不能自动发现结构性突变（如疫情冲击）；权重依赖历史回测区间\n"
            "· 局限：对数据长度敏感，历史不足 24 个月时建议改用单模型\n\n"
            "【典型使用案例】\n"
            "· 某机场编制下一年度保障预算：导入近 5 年月度客流 → 自动推荐权重 →\n"
            "  输出 12 个月预测与年度总量，作为人员与设备投入的测算依据"
        ),
    },
    {
        "id": "hw",
        "name": "Holt-Winters 指数平滑",
        "short": "经典季节平滑：结构简单、解释直观、计算极快",
        "content": (
            "【适用数据特征】\n"
            "· 具有稳定水平、趋势与年度季节性的月度数据（至少 24~36 个月）\n"
            "· 序列无明显突变、季节形态比较稳定的场景\n\n"
            "【预测周期与业务场景】\n"
            "· 适合 1~12 个月预测；短周期（1~3 个月）表现尤其稳健\n"
            "· 典型场景：月度滚动预测、资源弹性调配、快速测算\n\n"
            "【算法原理要点】\n"
            "· 由三个指数平滑方程组成：水平 L、趋势 T、季节 S\n"
            "· 预测值 = (L + T×h) × S（乘法季节）或 (L + T×h) + S（加法季节）\n"
            "· 平滑参数（α、β、γ）由优化器自动估计，无需手工调参\n\n"
            "【参数设置建议】\n"
            "· 季节周期：月度数据固定为 12\n"
            "· 趋势/季节类型：默认加法/加法；若数据呈乘性波动（淡旺季差异逐年放大）\n"
            "  可试乘法（mul）\n"
            "· 自动调参：数据 ≥ 48 个月时建议开启，以 AIC 选择最优组合\n\n"
            "【优缺点与局限性】\n"
            "· 优点：模型透明、可解释性强（水平/趋势/季节三项可直接向业务解释）\n"
            "· 优点：计算开销极小，回测速度快\n"
            "· 局限：只捕捉固定形态的季节性，无法引入宏观/行业外生变量\n"
            "· 局限：对结构性突变（重大事件冲击）反应滞后\n\n"
            "【典型使用案例】\n"
            "· 民航局月度数据快查：用近 3 年数据预测下季度各月客流，作为\n"
            "  航班加班申请的初步判断依据"
        ),
    },
    {
        "id": "sarima",
        "name": "SARIMA / SARIMAX",
        "short": "统计推断型：可引入 GDP、ASK 外生因子，适合中长期预测",
        "content": (
            "【适用数据特征】\n"
            "· 具备季节性与趋势的月度/季度数据，建议 36 个月以上\n"
            "· 需要结合宏观经济（GDP）与行业（ASK）因子共同建模时首选\n\n"
            "【预测周期与业务场景】\n"
            "· 适合 3~24 个月预测；中长期（6~24 个月）在精选集合中最具优势\n"
            "· 典型场景：年度经营计划、运力投放决策、与宏观环境联动分析\n\n"
            "【算法原理要点】\n"
            "· SARIMA(p,d,q)×(P,D,Q,m)：季节自回归-差分-移动平均模型\n"
            "· 对非平稳序列先做差分（d、D）再建模，捕捉自回归与移动平均结构\n"
            "· 接入外生变量后即 SARIMAX：y_t = ARIMA 部分 + β1×GDP_t + β2×ASK_t\n"
            "· 输出自带置信区间，可评估预测的不确定性\n\n"
            "【参数设置建议】\n"
            "· 月度数据默认 (1,1,1)×(1,1,1,12)；数据 ≥ 60 个月时开启自动调参\n"
            "· 外生变量：在管理控制台启用 GDP/ASK 接入后自动生效\n"
            "· 若收敛失败，系统自动回退到无外生变量的 SARIMA 或简单季节模型\n\n"
            "【优缺点与局限性】\n"
            "· 优点：理论基础扎实，能给出区间预测；可直接融合宏观/行业因子\n"
            "· 优点：外生变量系数可解释（如 ASK 每增长 1% 对客流的影响）\n"
            "· 局限：计算量大，自动调参时耗时明显；对短序列（<24 个月）不稳定\n"
            "· 局限：外生变量的未来值依赖外推假设，假设错误会传导到预测\n\n"
            "【典型使用案例】\n"
            "· 航空公司编制年度机队规划：启用管理控制台接入 GDP 与 ASK，\n"
            "  预测未来 18 个月旅客量，评估运力增长是否匹配市场容量"
        ),
    },
    {
        "id": "fallback",
        "name": "简单季节性模型（兜底）",
        "short": "数据不足时的保底方案：按历史同月均值外推",
        "content": (
            "【适用数据特征】\n"
            "· 历史数据不足 12~24 个月的极短序列\n"
            "· 或主力模型拟合失败（收敛异常）时的自动降级路径\n\n"
            "【预测周期与业务场景】\n"
            "· 仅建议 1~6 个月短期估算；用于系统不中断运行，不作为正式决策依据\n"
            "· 典型场景：新航线/新机场早期数据不足时的粗估\n\n"
            "【算法原理要点】\n"
            "· 以历史各月均值作为对应月份的预测值（月度季节性表）\n"
            "· 若某月无历史记录，则用整体均值替代\n\n"
            "【参数设置建议】\n"
            "· 无参数；当数据充足后系统会自动切换回精选模型\n\n"
            "【优缺点与局限性】\n"
            "· 优点：永不失败、零参数、可解释\n"
            "· 局限：不含趋势与波动信息，精度有限；仅作兜底\n\n"
            "【典型使用案例】\n"
            "· 支线机场开通首年：仅有 8 个月数据，系统自动使用简单季节模型\n"
            "  输出参考性预测，并在报告中标注“降级提示”"
        ),
    },
]

# 已移除算法说明（供界面展示移除依据）
REMOVED_NOTES: List[Dict[str, str]] = [
    {
        "id": "linear",
        "name": "线性回归（V1.1 已移除）",
        "content": (
            "【移除依据】\n"
            "· 线性回归假设序列为“直线趋势 + 噪声”，无法刻画月度强季节性形态，\n"
            "  在民航客流数据上回测误差（MAPE）通常明显劣于 Holt-Winters 与 SARIMA\n"
            "· 外推结果对端点敏感：端点值微扰会导致整条预测线大幅漂移，\n"
            "  在生产保障与成本预算场景中不可靠\n"
            "· 与精选模型高度冗余：其“趋势信息”已被 HW 的趋势项与 SARIMA 的\n"
            "  差分结构覆盖，保留它只增加融合中的噪声分量\n"
            "· 历史回测实证（V1.1 量化对比）支持上述结论，详见 docs/ALGORITHM_REDESIGN.md"
        ),
    },
]


def get_manual(manual_id: str) -> Dict[str, str]:
    for m in MANUALS:
        if m["id"] == manual_id:
            return m
    raise KeyError(f"未找到手册: {manual_id}")


def manual_names() -> List[str]:
    return [m["name"] for m in MANUALS]
