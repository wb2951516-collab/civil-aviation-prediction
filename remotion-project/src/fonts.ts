import { loadFont } from "@remotion/google-fonts/Inter";

export const { fontFamily: interFont } = loadFont("normal", {
  weights: ["400", "700"],
  subsets: ["latin"],
});

export const { fontFamily: notoSansSC } = loadFont("normal", {
  weights: ["400", "700"],
  subsets: ["latin"],
});
